const path = require('path');

module.exports = {
  'config': path.resolve('./config', 'config.json'),
  "models-path": path.resolve('./src/domain/model'),
  'seeders-path': path.resolve('./config'),
  'migrations-path': path.resolve('./config')
}