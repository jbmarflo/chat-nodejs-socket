module.exports = class UserRegisterRequest {
	
	constructor (userId, 	) {
		this.userId = userId
		this.groupId = groupId
		this.text = text 

		this.validate()
	}

	validate () {
		if (this.text.lenght > 300) {
			
		}
	}

}